# /etc/sses/sses.env -- runtime environment for the SSES service.
# Copy to /etc/sses/sses.env on the droplet, fill the connection string,
# chown root:synar, chmod 640.

ConnectionStrings__Synar=Host=localhost;Port=5432;Database=synar;Username=synar_app;Password=REPLACE_ME
